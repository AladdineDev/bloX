import 'package:flutter/material.dart';

class XHeader extends StatelessWidget {
  const XHeader({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Image.asset('./assets/images/x.png', height: 24,));
  }
}
