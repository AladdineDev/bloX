import 'package:flutter/material.dart';

class TweetListTabFollowing extends StatelessWidget {
  const TweetListTabFollowing({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("TweetListTabFollowing"),
    );
  }
}
